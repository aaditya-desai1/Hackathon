# DataVizPro Environment Variables

# MongoDB Connection String (REQUIRED)
# For Vercel deployment, this must be set in the Vercel project settings
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/datavizpro?retryWrites=true&w=majority

# Node Environment
NODE_ENV=development

# Port for local development
PORT=5000

# JWT Secret for authentication (if used)
JWT_SECRET=your_jwt_secret_here

# Instructions for Vercel deployment:
# 1. Make sure to add MONGODB_URI as an environment variable in your Vercel project settings
# 2. Use a MongoDB Atlas connection string or other cloud MongoDB provider
# 3. Ensure that your MongoDB instance is accessible from Vercel's deployment regions 