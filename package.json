{
  "name": "dataviz-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "fs-extra": "^11.1.1"
  }
} 