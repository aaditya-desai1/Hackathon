{
  "name": "dataviz-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run vercel-build"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  }
} 