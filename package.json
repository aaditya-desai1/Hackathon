{
  "name": "dataviz-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "node build-script.js",
    "start": "cd frontend && npm start",
    "build": "node build-script.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=16.x"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "fs-extra": "^11.1.1"
  }
} 