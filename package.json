{
  "name": "dataviz-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install react-scripts@5.0.1 --no-save",
    "vercel-build": "node scripts/build-script.js",
    "start": "cd frontend && npm start",
    "build": "node scripts/build-script.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "clean": "rm -rf build node_modules frontend/node_modules backend/node_modules",
    "dev": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=16.x"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  },
  "outputDirectory": "build"
} 